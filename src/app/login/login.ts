export class Login {
    loginId: string;
    pswd: string;
      constructor(
        loginId: string,
        pswd: string,
        ) {
          this.loginId = loginId;
          this.pswd = pswd;
        }
}